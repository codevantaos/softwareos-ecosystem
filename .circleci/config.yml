version: 2.1

# softwareos-base uses GitHub Actions for CI/CD.
# This CircleCI config provides a no-op workflow to prevent CircleCI
# from reporting "error" commit status on every PR.
# The actual CI/CD pipeline is defined in .github/workflows/ci.yaml

jobs:
  noop:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "No-op — CI handled by GitHub Actions"
          command: echo "softwareos-base CI/CD is managed by GitHub Actions."

workflows:
  ci-placeholder:
    jobs:
      - noop
